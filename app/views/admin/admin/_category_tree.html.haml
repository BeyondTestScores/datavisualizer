.category-tree
  %h4
    All Categories
    - if local_assigns[:show_questions]
      and Questions

  - Category.root.each do |c|
    .category
      = render "category_#{template}", category: c
      = render "child_categories", category: c, template: template, show_questions: local_assigns[:show_questions]
