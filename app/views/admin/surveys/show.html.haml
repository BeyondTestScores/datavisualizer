%h2= @survey.name

%br

%p
  = link_to("Edit Survey", edit_admin_survey_path(@survey))
  &nbsp; | &nbsp;
  = link_to "Delete Survey", [:admin, @survey],  method: :delete, data: { confirm: 'Are you sure you want to delete this survey?' }

%br

.category-tree
  %h4
    Questions In This Survey

  - @survey.category_tree[:children].keys.sort.each do |category_name|
    - category = @survey.category_tree[:children][category_name]
    .category
      = render "category_selector", category: category[:category]
      = render "child_categories", category: category, template: "selector", show_questions: true
