%h2= @survey.name

%br
%p
  - if (survey_monkey_details = @survey.survey_monkey_details).present? && survey_monkey_details['summary_url'].present?
    = link_to "Survey Monkey", survey_monkey_details['summary_url'], target: '_blank'
  - else
    Survey Monkey Error

%p
  = link_to("Edit Survey", edit_admin_survey_path(@survey))
  &nbsp; | &nbsp;
  = link_to "Delete Survey", [:admin, @survey],  method: :delete, data: { confirm: 'Are you sure you want to delete this survey?' }

%br

.tree
  %h4
    Questions In This Survey

  - @survey.tree[:child_categories].each do |child_category|
    .category{data: {category_id: child_category[:category].id}}
      = render "category_link", category: child_category[:category]
      = render 'category_questions', category: child_category, template: "link"
      = render "child_categories", category: child_category, template: "link", show_questions: true
