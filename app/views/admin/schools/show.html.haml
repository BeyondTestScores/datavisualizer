%h2= @school.name

%p
  = link_to("Edit School", edit_admin_school_path(@school))
  &nbsp; | &nbsp;
  = link_to "Delete School", [:admin, @school],  method: :delete, data: { confirm: 'Are you sure you want to delete this school?' }
